import React from 'react';

const Special = ({ring}) => {
    return (
        <div>
            <h4>Special</h4>
            <p><small>ring : {ring}</small></p>
        </div>
    );
};

export default Special;